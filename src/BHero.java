import java.util.Arrays;
import java.util.List;
import java.util.*;
import java.util.stream.Collectors;

public class BHero {

    public static void main(String[] args) {
/*
        String s1 = "Vimala"; String s2 = "Vimala";
        Map<String, Integer> map = new HashMap<String, Integer>();
        map.put(s1,20);
        map.put(s2,30);
        map.size();
        //map.count();
        Employee e1= new Employee("Vimala", "Mandadi");
        Employee e2= new Employee("Vimala", "Mandadi");
        Set<Employee> set = new HashSet<Employee>();
        set.add(e1);
        set.add(e2);
        set.size();
       // set.count();

        List<String> name= Arrays.asList("sajal","satyam","Sahil");
        List<String> result=name.stream().filter(s->s.startsWith("Sa")).collect(Collectors.toList());

        System.out.println(result);
*/

    }
}
